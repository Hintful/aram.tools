apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrpyt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: 'choym0098@gmail.com'
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
      - http01:
        ingress:
          class: nginx